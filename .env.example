# Optional: OpenAI-compatible chat completions endpoint. Keep keys on the server.
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=
AI_MODEL=
PORT=3100
# This MVP is local only. Demo role switching is not production authentication.
HOST=127.0.0.1
